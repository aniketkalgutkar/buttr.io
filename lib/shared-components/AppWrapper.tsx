"use client"
import { ReactNode, RefObject, useRef } from "react";
import { useScroll } from "framer-motion";

export default function getAppWrapper (handleMouseMove: (e: React.MouseEvent) => void ) {
    

    // const Wrapper = ({children}: {children: ReactNode}) => 
    //     ( 
            
    //     )

    // return {Wrapper: Wrapper, scrollYProgress: scrollYProgress}
}